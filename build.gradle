repositories {
  jcenter()
}

version = currentVersion

apply plugin: 'java'

dependencies {
    implementation 'com.google.inject:guice:5.1.0:no_aop'
    implementation 'com.google.inject:guice:5.1.0:javadoc'
}
