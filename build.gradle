repositories {
  jcenter()
}

version = currentVersion

apply plugin: 'java'

dependencies {
    implementation 'com.google.inject:guice:7.0.0:no_aop'
    implementation 'com.google.inject:guice:7.0.0:javadoc'
}
